# 🚀 Системы обновления и загрузки DNS

## Что реализовано

### 1️⃣ Автоматическая загрузка DNS с GitHub (Кнопка 📡)

**Как работает:**
- Пользователь нажимает кнопку 📡 в footer приложения
- Приложение подключается к GitHub и загружает файл `dns-servers.json`
- Список DNS обновляется БЕЗ перезагрузки приложения
- Показывается уведомление с количеством загруженных DNS

**Преимущества:**
- ✅ Добавлять новые DNS просто редактируя JSON файл
- ✅ Не требует переустановки приложения
- ✅ Легко добавлять DNS, которые нужны пользователям

### 2️⃣ Проверка обновлений приложения (Кнопка 🔄)

**Как работает:**
- Пользователь нажимает кнопку 🔄 в footer приложения
- Приложение проверяет GitHub Releases
- Если есть новая версия, показывает уведомление с ссылкой на скачивание
- Также проверяет обновления автоматически при запуске приложения

**Преимущества:**
- ✅ Пользователи всегда знают о новых версиях
- ✅ Могут скачать обновление в один клик
- ✅ Автоматическая проверка при запуске

### 3️⃣ Уведомления (Toast Notifications)

- Зеленые ✅ для успеха
- Красные ❌ для ошибок
- Синие ℹ️ для информации
- Исчезают через 3-8 секунд

## Как настроить для вашего GitHub

### Шаг 1: Отредактируйте renderer.js

Откройте `src/renderer/renderer.js` (строка ~5-11) и замените:

```javascript
const GITHUB_CONFIG = {
  username: 'YOUR_USERNAME',    // ← Ваше имя GitHub
  repo: 'YOUR_REPO',            // ← Название репозитория
  branch: 'main'                // ← Ветка (обычно main)
};
```

Пример:
```javascript
const GITHUB_CONFIG = {
  username: 'john-doe',
  repo: 'dns-shield-pro',
  branch: 'main'
};
```

### Шаг 2: Инициализируйте Git репозиторий

```bash
cd "e:\Cursor Project\Вместо впн приложение"
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### Шаг 3: Создайте GitHub Release

1. Перейдите в **Releases** вашего репозитория
2. Нажмите **Create a new release**
3. Заполните:
   - **Tag**: `v1.0.0`
   - **Title**: `DNS Shield Pro v1.0.0`
   - **Description**: Опишите изменения

4. Загрузите файл: `dist/DNS-Shield-Pro-win32-x64/DNS-Shield-Pro.exe`
5. Нажмите **Publish release**

## Как обновлять DNS

### Когда нужно добавить новые DNS серверы:

1. Отредактируйте `dns-servers.json` в вашем репозитории
2. Добавьте новый объект сервера (скопируйте структуру существующего)
3. Обновите поля:
   - `version` — увеличьте на 0.0.1
   - `timestamp` — обновите текущую дату/время

Пример добавления нового DNS:

```json
{
  "id": "mynewdns",
  "name": "My New DNS",
  "icon": "🟨",
  "description": {
    "en": "My DNS description",
    "ru": "Описание моего DNS"
  },
  "protocols": {
    "doh": {
      "url": "https://dns.example.com/dns-query",
      "name": "DNS-over-HTTPS",
      "platforms": ["Windows", "Browsers", "iOS", "Android"]
    },
    "dot": {
      "hostname": "dns.example.com",
      "name": "DNS-over-TLS",
      "platforms": ["Android", "Linux", "iOS"]
    },
    "ip": "1.2.3.4"
  },
  "features": {
    "en": ["Feature 1", "Feature 2"],
    "ru": ["Особенность 1", "Особенность 2"]
  }
}
```

4. Закоммитьте и запушьте:

```bash
git add dns-servers.json
git commit -m "Add new DNS server - Example DNS"
git push origin main
```

5. Пользователи нажимают 📡 → новые DNS загружаются автоматически!

## Как обновлять приложение

### Когда нужна новая версия приложения:

1. **Отредактируйте код** (добавьте новые функции, исправьте баги)

2. **Обновите версию** в `package.json`:
   ```json
   "version": "1.0.1"
   ```

3. **Пересоберите приложение**:
   ```bash
   npm run build:unpacked
   ```

4. **Создайте GitHub Release**:
   - Tag: `v1.0.1`
   - Title: `DNS Shield Pro v1.0.1`
   - Description: Что изменилось
   - Загрузите `dist/DNS-Shield-Pro-win32-x64/DNS-Shield-Pro.exe`

5. **Пользователи увидят**:
   - Уведомление "🆕 Новая версия: v1.0.1"
   - Кнопку для скачивания новой версии

## Структура файлов

```
dns-shield-pro/
├── src/
│   ├── renderer/
│   │   ├── renderer.js         ← Логика загрузки DNS и обновлений
│   │   ├── index.html          ← Кнопки 🔄 и 📡
│   │   ├── styles.css          ← Стили кнопок
│   │   └── config.js           ← CONFIG с DNS
│   └── main/
│       └── main.js
├── dns-servers.json            ← Список DNS для загрузки
├── package.json                ← Версия приложения
└── GITHUB_SETUP.md            ← Документация
```

## Функции в renderer.js

### `loadDNSServersFromGitHub()`
Загружает `dns-servers.json` с GitHub и обновляет список DNS

### `checkForUpdates(showNotification)`
Проверяет наличие новых Releases на GitHub

### Обработчики кнопок:
- `update-btn` — проверяет обновления приложения
- `reload-dns-btn` — перезагружает список DNS с GitHub

## Логирование

Все операции логируются в DevTools Console (F12):

```
[INFO] 📡 Fetching DNS servers from GitHub...
[INFO] ✅ DNS servers loaded from GitHub { count: 16, version: '1.0.1' }
[INFO] 🔄 Checking for app updates...
[INFO] 🆕 New version available { current: '1.0.0', latest: '1.0.1' }
```

## Помощь

### Если DNS не загружаются:
1. Проверьте, что репозиторий **публичный**
2. Убедитесь, что `dns-servers.json` в ветке `main`
3. Проверьте консоль (F12) на ошибки
4. Убедитесь, что GITHUB_CONFIG содержит правильные значения

### Если обновления не проверяются:
1. Проверьте, что создан GitHub Release с тегом `v1.0.X`
2. Убедитесь, что версия в Release больше чем в приложении
3. Проверьте консоль на ошибки

## Безопасность

⚠️ **Примечание:** Убедитесь, что:
- Репозиторий содержит только открытые данные
- DNS список содержит только проверенные серверы
- Не загружайте приватные ключи в репозиторий

---

**Готово!** Теперь у вас есть полноценная система автоматического обновления DNS и приложения. 🎉
