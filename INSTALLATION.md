# 📥 Инструкция по установке NetGuard

**DNS без компромиссов** — Установка за 2 минуты!

---

## 🚀 Быстрая установка (рекомендуется)

### Для Windows 10+

#### 1️⃣ Скачайте установщик
Перейдите на [Releases](https://github.com/PLGames/NetGuard/releases) и скачайте:
- **NetGuard-Setup.exe** (если хотите установить)
- **NetGuard-Portable.zip** (если хотите портативную версию)

#### 2️⃣ Запустите установщик
```bash
NetGuard-Setup.exe
```
- Нажмите "Next" → "Install" → "Finish"
- Приложение будет установлено в `C:\Program Files\NetGuard\`

#### 3️⃣ Запустите приложение
- Найдите иконку 🌐 NetGuard на рабочем столе
- Или запустите из меню "Пуск"
- Или запустите прямо из папки `C:\Program Files\NetGuard\NetGuard.exe`

#### 4️⃣ Подключитесь к DNS
- Нажмите на большой круг в центре экрана
- Ждите "Подключено" (max 5 сек)
- Готово! ✅

---

## 📦 Портативная версия (для флешки)

### Когда использовать
- Не хотите устанавливать на ПК
- Нужна версия на флешке
- Используете школьный/офисный компьютер без прав администратора

### Как использовать

#### 1️⃣ Скачайте архив
Скачайте **NetGuard-Portable.zip** с [Releases](https://github.com/PLGames/NetGuard/releases)

#### 2️⃣ Распакуйте архив
```bash
# Windows 10+ встроенный архиватор:
# 1. Правый клик на NetGuard-Portable.zip
# 2. "Извлечь все..."
# 3. Выберите папку назначения

# Или используйте 7-Zip/WinRAR
7z x NetGuard-Portable.zip
```

#### 3️⃣ Запустите приложение
```bash
# Перейдите в папку NetGuard-win32-x64/
# Запустите: NetGuard.exe
```

#### 4️⃣ Используйте как обычно
- Все работает так же как установленная версия
- Никаких файлов в системе не создается
- Можно скопировать на флешку и взять с собой!

---

## 🔧 Установка из исходников

### Требования
- **Node.js** 16+ ([Скачать](https://nodejs.org/))
- **npm** (устанавливается вместе с Node.js)
- **Git** ([Скачать](https://git-scm.com/))
- **Windows 10+**

### Процесс установки

#### 1️⃣ Клонируйте репозиторий
```bash
git clone https://github.com/PLGames/NetGuard.git
cd NetGuard
```

#### 2️⃣ Установите зависимости
```bash
npm install
```
Ждите завершения (может занять 2-5 минут)

#### 3️⃣ Запустите в режиме разработки
```bash
npm start
```
Приложение откроется и вы сможете разрабатывать!

#### 4️⃣ Соберите приложение
```bash
# Для распакованной версии (быстрая сборка)
npm run build:unpacked

# Или для создания установщика
npm run build
```

#### 5️⃣ Запустите собранное приложение
```bash
# Распакованная версия
dist/NetGuard-win32-x64/NetGuard.exe

# Или используйте установщик
dist/NetGuard-Setup.exe
```

---

## 🐛 Решение проблем при установке

### ❌ "Не могу скачать файл" / "Страница не найдена"

**Решение:**
1. Проверьте интернет соединение
2. Очистите кэш браузера (Ctrl+Shift+Delete)
3. Скачайте ещё раз с GitHub
4. Если проблема повторяется → [Создайте Issue](https://github.com/PLGames/NetGuard/issues)

### ❌ Windows SmartScreen / Антивирус блокирует

**Решение (Windows SmartScreen):**
1. При попытке запуска увидите "Защита Windows Defender"
2. Нажмите "Подробнее" → "Выполнить в любом случае"
3. Это нормально для новых приложений

**Решение (Антивирус):**
1. Добавьте исключение для папки `C:\Program Files\NetGuard\`
2. Или добавьте `NetGuard.exe` в белый список антивируса
3. Это открытый исходный код, вы можете проверить его сами!

### ❌ "Не могу распаковать архив"

**Решение:**
1. Убедитесь что используете встроенный архиватор Windows (правый клик → Извлечь)
2. Или скачайте [7-Zip](https://www.7-zip.org/) и распакуйте им
3. Если архив поврежден → перескачайте его

### ❌ "npm: команда не найдена"

**Решение:**
1. Установите [Node.js](https://nodejs.org/) (выберите LTS версию)
2. Перезагрузитесь после установки
3. Откройте новый Terminal (PowerShell) и повторите `npm install`
4. Если не помогает → установите Node.js **For All Users** (требует администратора)

### ❌ "Приложение не запускается" / Черный экран

**Решение:**
1. Откройте DevTools (F12) и посмотрите ошибки в консоли
2. Попробуйте удалить папку `%AppData%/NetGuard/`
3. Переустановите приложение
4. Если ошибка повторяется → [Создайте Issue с логами](https://github.com/PLGames/NetGuard/issues)

### ❌ "Не подключается к DNS" / "ENOTFOUND ошибка"

**Решение:**
1. Проверьте интернет соединение
2. Попробуйте другой DNS (нажмите "🔄 Сменить DNS")
3. Проверьте что не включен VPN (может блокировать DNS запросы)
4. Отключите временно брандмауэр и попробуйте
5. Если не помогает → [Создайте Issue](https://github.com/PLGames/NetGuard/issues)

---

## 🖥️ Системные требования

### Минимальные требования
- **OS:** Windows 10 (build 14393) или выше
- **Процессор:** Intel Core 2 Duo или AMD Athlon 64 (или новее)
- **RAM:** 2 GB минимум
- **Диск:** 200 MB свободного места
- **Интернет:** Стабильное соединение

### Рекомендуемые требования
- **OS:** Windows 11
- **Процессор:** Intel Core i5/i7 или AMD Ryzen 5/7 (2019+)
- **RAM:** 8 GB и выше
- **Диск:** SSD с 500 MB свободного места
- **Интернет:** WLAN или LAN с скоростью 50+ Mbps

### Совместимость
✅ Windows 10 Home  
✅ Windows 10 Pro  
✅ Windows 10 Enterprise  
✅ Windows 11 Home  
✅ Windows 11 Pro  
✅ Windows 11 Enterprise  
✅ Windows Server 2016+  
✅ Windows PE (WinPE)  

❌ Windows 7 (не поддерживается)  
❌ Windows 8/8.1 (не поддерживается)  

---

## 🗑️ Удаление приложения

### Удаление установленной версии

#### Способ 1: Через "Параметры" (Windows 11)
```
Параметры → Приложения → Приложения и возможности
→ Поиск "NetGuard"
→ Нажмите на NetGuard
→ "Удалить" → Подтвердите
```

#### Способ 2: Через "Программы и компоненты" (Windows 10)
```
Параметры → Приложения → Программы и компоненты
→ Поиск "NetGuard"
→ Правый клик на NetGuard
→ "Удалить"
```

#### Способ 3: Через программу удаления
```
C:\Program Files\NetGuard\Uninstall.exe
```

### Удаление портативной версии
Просто удалите папку `NetGuard-win32-x64/` 🗑️

### Очистка пользовательских данных
```
# Удалите папку настроек:
%AppData%/NetGuard/

# Или используйте командную строку (PowerShell):
Remove-Item -Path "$env:APPDATA\NetGuard" -Recurse -Force
```

---

## 🔄 Обновление приложения

### Автоматическое обновление
NetGuard проверяет обновления при каждом запуске:
1. Нажмите кнопку "🔄" в нижнем правом углу
2. Приложение проверит наличие обновлений
3. Если обновление есть → нажмите "Обновить"
4. Приложение перезагрузится с новой версией

### Ручное обновление
1. Скачайте новую версию с [Releases](https://github.com/PLGames/NetGuard/releases)
2. Запустите новый установщик (установленная версия) или распакуйте новый архив (портативная версия)
3. Старая версия будет заменена

### Обновление DNS списка
1. Нажмите кнопку "📡" в нижнем правом углу
2. Приложение загрузит свежий список DNS с GitHub
3. Новые серверы станут доступны сразу

---

## 💡 Советы после установки

### 1. Включите автоподключение
```
Нажмите ⚙️ → Настройки
→ Отметьте "Автоподключение"
→ Сохраните
```
Теперь NetGuard будет автоматически подключаться при запуске ПК!

### 2. Выберите предпочитаемый DNS
```
Нажмите на выпадающий список DNS
→ Выберите ваш любимый провайдер
→ Нажмите "Применить"
```

### 3. Протестируйте разные DNS
```
Если YouTube/Twitch не работает:
1. Нажмите "🔄 Сменить DNS"
2. Повторяйте пока сервис не заработает
3. Запомните какой DNS работает лучше
```

### 4. Добавьте в автозагрузку (опционально)
```
Windows + R → shell:startup
→ Создайте ярлык на NetGuard.exe
Теперь приложение будет запускаться вместе с Windows
```

---

## 📞 Нужна помощь?

- 🐛 **Нашли баг?** [Создайте Issue](https://github.com/PLGames/NetGuard/issues)
- 💡 **Идея для функции?** [Обсудите в Discussions](https://github.com/PLGames/NetGuard/discussions)
- 📧 **Другой вопрос?** [Email нас](mailto:support@plgames.dev)

---

**Готовы к свободному интернету?** 🌐

🎉 **Спасибо за установку NetGuard!**

DNS без компромиссов ✅
